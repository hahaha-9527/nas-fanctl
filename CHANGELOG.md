# Changelog

fanctl 的版本更新记录。版本号格式：`v<主>.<次>`，每次公开发布递增。

## v1.72 — 2026-09-14

### 新增

- **联动温度可选**：温度-转速曲线卡片新增「联动温度」分段按钮，每个风扇可独立选择按 **CPU 温度** 或 **硬盘温度** 调速，适配不同装机布局（如只接了一个风扇、或某路传感器读数为空的机器）。未选择的风扇保持原有自动匹配行为，老用户升级后无需重新配置。

### 修复

- **远程访问图标不显示**：通过反向代理（内网穿透）远程打开 ZeroNAS 桌面时，应用图标因登记的是内网 IP 而裂图。图标改由 TOS 网页目录提供，局域网 / 远程网页端 / 手机 APP 全场景均可加载。

### 说明

本版本起项目开源。

## v1.71 — 2026-09-14

### 修复

- **手机 APP 图标不刷新 / 变白**：原生图片加载器不识别带查询参数的 URL，改用独立路径 `/icon2.svg`。
- **应用中心应用列表整体加载失败**：安装包体积写入小数导致后端整型解析报错，现固定写入整数（单位 KB）。

### 变更

- 完善 ZeroNAS 应用中心注册信息：中英文详细描述、分版本功能列表、开发者署名、安装包与镜像体积。

## v1.70 — 2026-09-13

### 首个公开发行版

- **三档独立功率曲线**：自动 / 静音 / 均衡三档各自独立曲线并持久保存，全速为临时档，页头一键切换。
- **曲线可视化编辑**：网页端直接拖拽调整 温度→PWM 曲线拐点。
- **温度联动调速**：CPU 温度 + 全部在位 SATA 硬盘最高温，升温立即响应、降温缓降防抖、超温紧急全速保护。
- **一键调速与恢复默认**：档位随各风扇曲线等比适配；一键重置曲线与全局参数。
- **单风扇手动定速**：临时接管，切档自动释放。
- **驱动三层保障**：compose 启动加载 + 宿主机 systemd 服务 + 容器内自愈，重启不丢驱动。
- **桌面图标注册**：`tools/register_icon.py` 一键注册 ZeroNAS 桌面快捷图标与应用中心信息。
- **体验细节**：深浅色主题、页面 150% 缩放、手机端自适应。
- **安全保护**：0 PWM 通道检测拒绝执行；配置原子写入并自动备份。
