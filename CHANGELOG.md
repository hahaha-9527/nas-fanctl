# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)；版本号格式为 `v主版本.迭代版本`。

## [v1.72] — 2026-09-14

### 新增
- **联动温度可选**：温度-转速曲线卡片中新增「联动温度」分段按钮，每个风扇可独立选择按 **CPU 温度** 或 **硬盘温度** 调速。解决不同装机布局（如只接了一个风扇、或某路风扇未到启动转速导致传感器读数为空）下联动数据不准的问题。
- 未显式选择联动温度的风扇保持原有行为（自动匹配检测到的传感器），老用户升级后无需重新配置。

### 修复
- **远程访问图标不显示**：通过铁牛link 等反向代理远程打开 ZeroNAS 桌面时，应用图标因登记的是内网 IP 而裂图。图标文件改为部署到 TOS 网页目录并以公网可访问的绝对地址登记，局域网 / 远程网页端 / 手机 APP 全场景可加载。

## [v1.71] — 2026-09-14

### 修复
- **手机 APP 图标不刷新 / 变白**：原生 APP 的图片加载器不识别带查询参数的 URL（缓存穿透用的 `?v=2` 会被当作非法图片）。新增独立的 `/icon2.svg` 路径，图标地址不再依赖查询参数。
- **应用中心应用列表整体加载失败**：注册信息中的安装包体积写入了小数，导致后端整型解析失败、整个列表接口 500。该字段现固定写入整数（单位 KB）。

### 变更
- 完善 ZeroNAS 应用中心注册信息：详细中英文描述、分版本功能列表、开发者署名、安装包体积与镜像体积。

## [v1.70] — 2026-09-13

首个公开发行版本，包含此前内部迭代的全部功能。

### 新增
- **三档独立功率曲线**：自动 / 静音 / 均衡 三档各自拥有独立曲线并可持久保存；全速为临时档，重启后回到原档位。
- **一键调速**：页头分段按钮快速切换档位，切换时自动清理该风扇的手动覆盖值。
- **一键恢复默认**：重置三档曲线、档位与全部全局参数（保留 `enable_exec`），配置写入为原子操作并自动备份。
- **曲线可视化编辑**：网页端拖拽调整 温度→PWM 曲线拐点。
- **温度联动**：CPU 温度 + 全部在位 SATA 硬盘最高温（6 盘位就绪；NVMe 仅展示温度、不参与联动）。
- **驱动自愈**：`it87`（7 组 force_id）/ `nct6775` 自动加载；compose 启动加载 + 宿主机 systemd 服务 + 容器内自愈三层保障，重启不丢驱动。
- **单风扇手动定速**、**风扇命名与联动分组**、**深浅色主题**、**页面 150% 缩放**、**手机端自适应**。
- **ZeroNAS 桌面图标注册工具**：一键注册应用中心条目与桌面快捷方式。
- **安全保护**：检测到 0 个可用 PWM 通道时拒绝写入并保留原配置。

### 说明
- 版本号 `1.0` – `1.69` 为内部迭代，未公开发布，故不在此列出。
